package com.lansoftprogramming.runeSequence.core.sequence.runtime.modification;

/**
 * Indicates where a detected ability sits relative to the current sequence position.
 */
public enum StepPosition {
	CURRENT_STEP,
	NEXT_STEP,
	OTHER
}

